; =================================================================
; qBittorrent 自动上传助手 - 设置问卷
; =================================================================
; 你好！我是你的上传助手。请回答以下几个问题，好让我知道该如何工作。
; 你只需要修改等号 = 右边的部分。
; =================================================================


[Uploader]
; --- BaiduPCS-Go.exe 的程序路径 ---
; 如果你已经把 BaiduPCS-Go 添加到系统环境变量，直接写 "BaiduPCS-Go" 即可。
; 否则，请提供它的完整路径。
; 注意: 路径中的反斜杠 \ 必须写成两个 \\
; 示例: Path = C:\\Tools\\BaiduPCS-Go\\BaiduPCS-Go.exe
Path = BaiduPCS-Go

; --- 网盘上传目录 ---
; 下载的文件将被上传到这个网盘目录下的一个同名文件夹中。
; 注意：这里要使用百度网盘的绝对路径格式，以 / 开头。
; 示例: MyCloudFolder = /我的应用数据/qbuploader
MyCloudFolder = /qbuploader_backups

; --- 上传参数 ---
; 在这里可以填写额外的 BaiduPCS-Go 上传参数。
;   --parallel=N: 指定上传任务的并发数。
;   --no-rapid:   禁用秒传，强制上传。
; 建议保持默认，留空即可。
ExtraArgs =

[qBittorrent]
; --- qBittorrent Web UI 设置 ---
; 为了让助手能连接到qBittorrent，你需要开启它的Web用户界面。
; (在 qBittorrent -> 工具 -> 选项 -> Web UI 中设置)
Host = http://localhost:8080
Username = your_qb_username
Password = your_qb_password

[Seeding_Policy]
; --- 清理目标状态 ---
; 当一个任务在 qBittorrent 中处于以下任一状态时，
; 并且它已经被成功上传，程序才会尝试清理它。
; 多个状态用逗号分隔，不区分大小写。
; 常见状态: pausedUP, stalledUP
Cleanup_Target_States = pausedUP, stalledUP

; --- 任务完成后在 qBittorrent 中的操作 ---
; 当文件上传和清理都完成后，如何处理 qBittorrent 里的任务记录。
;   "delete":       彻底删除任务和种子。
;   "pause":        暂停任务。(此选项在当前逻辑下意义不大)
;   "do_nothing":   什么也不做。
Action_After_Process = delete

[General]
; --- 日志记录模式 ---
; 当我工作时，你希望我把工作日记写得多详细？
;   "normal": 只记录重要步骤。(推荐)
;   "debug":  记录所有细节，用于排查问题。
Log_Mode = normal

[Maintenance]
; --- 自动维护设置 ---
Log_Max_Size_MB = 10
Log_Max_Backups = 5
DB_Keep_Archived_Days = 365